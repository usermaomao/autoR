# autoR 微信小程序移植可行性评估报告

> 评估日期：2025-01-27
> 评估版本：autoR v2.0
> 评估目标：分析将现有 Vue 3 + Django 项目移植到微信小程序的可行性、工作量与技术方案

---

## 一、执行摘要

### 总体评估结论

| 评估维度 | 评分 | 说明 |
|---------|------|------|
| **技术可行性** | ⭐⭐⭐⭐ (80%) | 技术上完全可行，但需要重写前端 |
| **直接移植性** | ⭐ (10%) | **不能直接移植**，需要完全重构前端 |
| **工作量估算** | 中高 | 预计 4-6 周（1人全职） |
| **功能完整度** | ⭐⭐⭐⭐ (85%) | 大部分功能可实现，少数需要替代方案 |

### 核心结论

```
❌ 不能直接移植 —— Vue 3 代码无法在微信小程序中运行
✅ 可以移植 —— 通过以下方式实现：
   方案 A: 使用 uni-app 重写前端（推荐）
   方案 B: 使用 Taro 重写前端
   方案 C: 原生微信小程序开发
```

---

## 二、技术栈对比分析

### 2.1 前端技术栈对比

| 技术项 | 当前项目 | 微信小程序 | 兼容性 | 移植方案 |
|--------|---------|-----------|--------|---------|
| **框架** | Vue 3 | WXML + WXSS + JS | ❌ 不兼容 | uni-app / Taro / 原生重写 |
| **路由** | vue-router | 小程序页面栈 | ❌ 不兼容 | 使用小程序 navigateTo/switchTab |
| **状态管理** | Pinia | 无内置 | ❌ 不兼容 | 使用 Vuex (uni-app) 或 MobX |
| **HTTP 客户端** | Axios | wx.request | ❌ 不兼容 | 封装 wx.request |
| **CSS 框架** | Tailwind CSS | WXSS | ⚠️ 部分兼容 | 需要转换为 rpx 单位 |
| **图表库** | Chart.js | 无内置 | ❌ 不兼容 | 使用 wx-canvas / ECharts-miniprogram |
| **CSV 解析** | PapaParse | 无 | ❌ 不兼容 | 手动实现或小程序版本 |

### 2.2 后端技术栈对比

| 技术项 | 当前项目 | 小程序要求 | 兼容性 | 移植方案 |
|--------|---------|-----------|--------|---------|
| **后端框架** | Django 5 | 任意 | ✅ 兼容 | 无需修改 |
| **API 格式** | REST JSON | REST JSON | ✅ 兼容 | 无需修改 |
| **认证方式** | Session Cookie | 需调整 | ⚠️ 需修改 | 改为 Token/JWT + 微信登录 |
| **域名要求** | localhost | **HTTPS + 备案域名** | ⚠️ 需配置 | 购买域名 + SSL + ICP 备案 |
| **数据库** | SQLite | 无要求 | ✅ 兼容 | 生产环境建议升级 PostgreSQL |

### 2.3 核心功能兼容性

| 功能模块 | 兼容性 | 移植难度 | 说明 |
|---------|--------|---------|------|
| **用户注册/登录** | ⚠️ 需调整 | 中 | 需接入微信登录 |
| **卡片 CRUD** | ✅ 兼容 | 低 | API 不变，UI 重写 |
| **复习功能** | ⚠️ 需调整 | 中 | 键盘快捷键需改为触摸手势 |
| **闪卡翻转动画** | ⚠️ 需调整 | 中 | CSS 3D 需用小程序动画 API |
| **SVG 渲染** | ❌ 不兼容 | 高 | 需转为 Canvas 或图片 |
| **导入/导出** | ⚠️ 部分支持 | 高 | 文件系统访问受限 |
| **统计图表** | ⚠️ 需调整 | 中 | 使用 ECharts 小程序版 |
| **AI 功能** | ✅ 兼容 | 低 | API 调用不变 |

---

## 三、核心技术挑战详解

### 3.1 挑战一：Vue 3 无法直接运行

**问题描述**：
```
微信小程序有自己的视图层（WXML）和逻辑层（JS），
不支持 DOM 操作，不能运行 Vue 3 的虚拟 DOM。

当前代码：
<template>
  <div class="flashcard" :class="{ flipped: isFlipped }">
    <div v-if="hasSVG">...</div>
  </div>
</template>

小程序要求：
<view class="flashcard {{isFlipped ? 'flipped' : ''}}">
  <view wx:if="{{hasSVG}}">...</view>
</view>
```

**解决方案**：

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| **uni-app** | Vue 语法、一码多端、生态成熟 | 性能略低于原生 | ⭐⭐⭐⭐⭐ |
| **Taro** | React/Vue 语法、京东维护 | 学习成本较高 | ⭐⭐⭐⭐ |
| **原生开发** | 性能最好、无框架限制 | 工作量最大、无法复用 | ⭐⭐⭐ |

**推荐方案：uni-app**

```javascript
// uni-app 可以保留大部分 Vue 语法
// 当前 Vue 3 代码
<template>
  <div class="flashcard" :class="{ flipped: isFlipped }">
    <FlashCard :card="currentCard" @rate="handleRate" />
  </div>
</template>

// uni-app 代码（几乎相同）
<template>
  <view class="flashcard" :class="{ flipped: isFlipped }">
    <FlashCard :card="currentCard" @rate="handleRate" />
  </view>
</template>
```

### 3.2 挑战二：认证方式不兼容

**问题描述**：
```
当前项目使用 Django Session + Cookie 认证
微信小程序不支持 Cookie，需要改为 Token 认证 + 微信登录
```

**当前认证流程**：
```
用户名/密码 → Django Session → Cookie → 后续请求自动携带
```

**小程序认证流程**：
```
wx.login() → code → 后端换取 openid → 生成 JWT Token → 存储到 Storage → 请求头携带
```

**解决方案**：

```python
# 后端新增微信登录接口 (backend/cards/views.py)

from rest_framework_simplejwt.tokens import RefreshToken

class WeChatLoginView(APIView):
    permission_classes = [AllowAny]

    def post(self, request):
        code = request.data.get('code')

        # 调用微信 API 获取 openid
        wx_response = requests.get(
            'https://api.weixin.qq.com/sns/jscode2session',
            params={
                'appid': settings.WECHAT_APPID,
                'secret': settings.WECHAT_SECRET,
                'js_code': code,
                'grant_type': 'authorization_code'
            }
        )

        data = wx_response.json()
        openid = data.get('openid')

        # 查找或创建用户
        user, created = User.objects.get_or_create(
            username=f'wx_{openid}',
            defaults={'email': f'{openid}@wechat.local'}
        )

        # 生成 JWT Token
        refresh = RefreshToken.for_user(user)

        return Response({
            'access': str(refresh.access_token),
            'refresh': str(refresh),
            'user': {'id': user.id, 'username': user.username}
        })
```

```javascript
// 小程序端登录 (miniprogram/utils/auth.js)

export async function wechatLogin() {
  // 1. 获取微信登录 code
  const { code } = await wx.login()

  // 2. 发送到后端换取 token
  const res = await wx.request({
    url: 'https://your-domain.com/api/auth/wechat-login/',
    method: 'POST',
    data: { code }
  })

  // 3. 存储 token
  wx.setStorageSync('access_token', res.data.access)
  wx.setStorageSync('refresh_token', res.data.refresh)

  return res.data.user
}

// 请求拦截器
export function request(options) {
  const token = wx.getStorageSync('access_token')

  return wx.request({
    ...options,
    header: {
      ...options.header,
      'Authorization': token ? `Bearer ${token}` : ''
    }
  })
}
```

### 3.3 挑战三：SVG 渲染问题

**问题描述**：
```
当前项目使用 v-html 渲染 SVG 内容
微信小程序不支持 v-html，且对 SVG 支持有限
```

**当前实现**：
```vue
<!-- SVGCard.vue -->
<div v-html="svgContent" class="svg-content"></div>
```

**解决方案**：

| 方案 | 实现方式 | 优点 | 缺点 |
|------|---------|------|------|
| **Canvas 渲染** | 后端将 SVG 转为 Canvas 指令 | 性能好 | 开发复杂 |
| **图片转换** | 后端将 SVG 转为 PNG/WebP | 最简单 | 不够清晰、占用存储 |
| **rich-text** | 使用 rich-text 组件 | 部分支持 | SVG 标签支持不完整 |
| **WebView** | 嵌入 H5 页面 | 完全支持 | 体验不够原生 |

**推荐方案：后端生成图片 + 本地缓存**

```python
# 后端新增 SVG 转图片接口 (backend/cards/views.py)

import cairosvg
import base64

class CardViewSet(viewsets.ModelViewSet):

    @action(detail=True, methods=['get'])
    def svg_image(self, request, pk=None):
        """将 SVG 转换为 PNG 图片返回"""
        card = self.get_object()
        svg_content = card.metadata.get('svg_front', '')

        if not svg_content:
            return Response({'error': 'No SVG content'}, status=404)

        # SVG 转 PNG
        png_data = cairosvg.svg2png(bytestring=svg_content.encode())

        # 返回 base64 或直接返回图片
        return Response({
            'image': base64.b64encode(png_data).decode(),
            'format': 'png'
        })
```

```javascript
// 小程序端显示卡片图片
<template>
  <view class="flashcard">
    <image
      v-if="cardImage"
      :src="cardImage"
      mode="aspectFit"
      class="card-image"
    />
    <view v-else class="card-text">
      {{ card.word }}
    </view>
  </view>
</template>

<script>
export default {
  data() {
    return {
      cardImage: ''
    }
  },
  async onLoad() {
    // 获取卡片图片
    const res = await request({
      url: `/api/cards/${this.cardId}/svg_image/`
    })
    this.cardImage = `data:image/png;base64,${res.data.image}`
  }
}
</script>
```

### 3.4 挑战四：键盘快捷键替代

**问题描述**：
```
当前复习页面依赖键盘快捷键：
- 1/2/3/4 评分
- S 显示答案
- Z 撤销
- Enter 下一张

小程序无物理键盘，需要改为触摸交互
```

**解决方案：手势操作**

```javascript
// 小程序手势操作方案

// 1. 左右滑动评分
// 左滑 = 困难 (2)
// 右滑 = 简单 (5)

// 2. 点击翻转
// 单击 = 翻转卡片

// 3. 长按撤销
// 长按 = 撤销上一次

// 4. 双击进入下一张
// 双击 = 完成并进入下一张
```

```vue
<!-- uni-app 实现 -->
<template>
  <view
    class="flashcard"
    @tap="handleTap"
    @longpress="handleUndo"
    @touchstart="touchStart"
    @touchend="touchEnd"
  >
    <!-- 卡片内容 -->
  </view>

  <!-- 底部评分按钮（备选方案） -->
  <view class="rating-buttons">
    <button @tap="rate(0)" class="btn-again">再来</button>
    <button @tap="rate(2)" class="btn-hard">困难</button>
    <button @tap="rate(4)" class="btn-good">良好</button>
    <button @tap="rate(5)" class="btn-easy">简单</button>
  </view>
</template>

<script>
export default {
  data() {
    return {
      startX: 0,
      startY: 0
    }
  },
  methods: {
    touchStart(e) {
      this.startX = e.touches[0].clientX
      this.startY = e.touches[0].clientY
    },
    touchEnd(e) {
      const endX = e.changedTouches[0].clientX
      const endY = e.changedTouches[0].clientY
      const deltaX = endX - this.startX
      const deltaY = endY - this.startY

      // 水平滑动超过 100px 且垂直滑动小于 50px
      if (Math.abs(deltaX) > 100 && Math.abs(deltaY) < 50) {
        if (deltaX > 0) {
          // 右滑 = 简单
          this.rate(5)
        } else {
          // 左滑 = 困难
          this.rate(2)
        }
      }
    },
    handleTap() {
      this.isFlipped = !this.isFlipped
    },
    handleUndo() {
      uni.showModal({
        title: '撤销',
        content: '确定撤销上一次评分？',
        success: (res) => {
          if (res.confirm) {
            this.undoLastReview()
          }
        }
      })
    }
  }
}
</script>
```

### 3.5 挑战五：域名与备案要求

**问题描述**：
```
微信小程序要求：
1. 所有网络请求必须使用 HTTPS
2. 域名必须经过 ICP 备案
3. 域名需在小程序后台配置白名单
```

**解决方案清单**：

| 步骤 | 内容 | 预估时间 | 预估费用 |
|------|------|---------|---------|
| 1. 购买域名 | 如 autor.com | 1天 | ¥50-100/年 |
| 2. ICP 备案 | 提交工信部备案 | 7-20天 | 免费 |
| 3. 购买服务器 | 阿里云/腾讯云 | 1天 | ¥500-2000/年 |
| 4. SSL 证书 | Let's Encrypt 免费证书 | 1天 | 免费 |
| 5. 配置 Nginx | HTTPS + 反向代理 | 1天 | - |
| 6. 小程序后台配置 | 添加合法域名 | 1小时 | - |

**Nginx 配置示例**：

```nginx
server {
    listen 443 ssl;
    server_name api.autor.com;

    ssl_certificate /etc/letsencrypt/live/autor.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/autor.com/privkey.pem;

    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static/ {
        alias /var/www/autor/static/;
    }
}
```

---

## 四、移植方案对比

### 4.1 方案 A：uni-app 重写（推荐）

```
优点：
✅ 保留 Vue 语法，学习成本低
✅ 一套代码可发布到多端（微信/支付宝/H5/App）
✅ 丰富的组件库和插件生态
✅ 可复用大部分业务逻辑代码

缺点：
❌ 性能略低于原生
❌ 部分复杂 CSS 效果需要调整
❌ 框架更新可能有兼容问题
```

**代码复用率评估**：

| 代码类型 | 复用率 | 说明 |
|---------|--------|------|
| **Vue 模板** | 70% | 将 div 改为 view，其他基本相同 |
| **Vue 逻辑** | 90% | 大部分可直接复用 |
| **CSS 样式** | 50% | Tailwind 需转为 WXSS |
| **API 调用** | 80% | 改用 uni.request |
| **Pinia Store** | 95% | 几乎完全复用 |
| **工具函数** | 100% | 完全复用 |

### 4.2 方案 B：Taro 重写

```
优点：
✅ 支持 React 和 Vue
✅ 京东团队维护，稳定可靠
✅ 性能优化较好

缺点：
❌ 语法与 Vue 3 有差异
❌ 生态不如 uni-app 丰富
❌ 学习成本较高
```

### 4.3 方案 C：原生小程序开发

```
优点：
✅ 性能最好
✅ 无框架限制
✅ 官方支持最完整

缺点：
❌ 无法复用 Vue 代码
❌ 工作量最大
❌ 只能发布微信平台
```

### 方案选择建议

```
推荐：方案 A (uni-app)

理由：
1. 当前项目使用 Vue 3，uni-app 复用率最高
2. 一码多端，未来可扩展到其他平台
3. 社区活跃，遇到问题容易解决
4. 开发效率高，预估可节省 30% 时间
```

---

## 五、模块移植详细计划

### 5.1 模块移植优先级

| 优先级 | 模块 | 工作量 | 依赖 |
|--------|------|--------|------|
| P0 | 用户认证（微信登录） | 3天 | 无 |
| P0 | 卡片列表与详情 | 2天 | 认证 |
| P0 | 复习核心功能 | 4天 | 卡片 |
| P1 | 卡片创建/编辑 | 3天 | 卡片 |
| P1 | 字典查询自动填充 | 2天 | 卡片创建 |
| P2 | 统计图表 | 3天 | 复习 |
| P2 | 导入/导出 | 2天 | 卡片 |
| P3 | AI 功能 | 2天 | 卡片创建 |
| P3 | 卡组管理 | 1天 | 卡片 |

### 5.2 各模块移植说明

#### 模块 1：用户认证

**原始实现**：
- Session Cookie 认证
- 用户名/密码登录

**小程序实现**：
- 微信一键登录
- JWT Token 认证
- 可选：手机号绑定

**后端改动**：
```python
# 新增依赖
djangorestframework-simplejwt==5.3.1

# 新增 URL
urlpatterns += [
    path('api/auth/wechat-login/', WeChatLoginView.as_view()),
    path('api/auth/token/refresh/', TokenRefreshView.as_view()),
]
```

#### 模块 2：复习功能

**原始实现**：
- 键盘快捷键评分
- CSS 3D 翻转动画
- SVG 卡片渲染

**小程序实现**：
- 触摸手势 + 按钮评分
- 小程序动画 API 翻转
- Canvas/图片渲染

**关键代码转换**：

```javascript
// Vue 3 原始代码
onMounted(() => {
  window.addEventListener('keydown', handleKeydown)
})

// uni-app 转换后
onReady() {
  // 无需键盘事件，使用触摸事件
}
```

```css
/* Vue 3 原始 CSS */
.flashcard-inner {
  transition: transform 0.6s;
  transform-style: preserve-3d;
}
.flashcard.flipped .flashcard-inner {
  transform: rotateY(180deg);
}

/* 小程序 WXSS + 动画 API */
.flashcard-inner {
  /* 使用 animation 属性绑定 */
}
```

```javascript
// 小程序动画实现
flipCard() {
  const animation = uni.createAnimation({
    duration: 600,
    timingFunction: 'ease'
  })

  if (this.isFlipped) {
    animation.rotateY(0).step()
  } else {
    animation.rotateY(180).step()
  }

  this.animationData = animation.export()
  this.isFlipped = !this.isFlipped
}
```

#### 模块 3：统计图表

**原始实现**：
- Chart.js 渲染

**小程序实现**：
- ECharts 小程序版
- wx-canvas 自定义绘制

**依赖安装**：
```bash
npm install echarts-for-weixin
```

```vue
<!-- 小程序图表组件 -->
<template>
  <view class="chart-container">
    <ec-canvas
      id="chart"
      canvas-id="chart"
      :ec="ec"
    />
  </view>
</template>

<script>
import * as echarts from 'echarts-for-weixin'

export default {
  data() {
    return {
      ec: {
        onInit: this.initChart
      }
    }
  },
  methods: {
    initChart(canvas, width, height, dpr) {
      const chart = echarts.init(canvas, null, {
        width, height, devicePixelRatio: dpr
      })

      chart.setOption({
        xAxis: { type: 'category', data: this.dates },
        yAxis: { type: 'value' },
        series: [{
          data: this.values,
          type: 'line',
          smooth: true
        }]
      })

      return chart
    }
  }
}
</script>
```

---

## 六、工作量估算

### 6.1 总体工时估算

| 阶段 | 任务 | 工时（人天） |
|------|------|-------------|
| **准备阶段** | 环境搭建、uni-app 项目初始化 | 1 |
| | 域名购买、备案、服务器配置 | 3（含等待时间 7-20 天） |
| | 后端微信登录接口开发 | 2 |
| **开发阶段** | 用户认证模块 | 2 |
| | 卡片列表/详情 | 3 |
| | 复习核心功能 | 5 |
| | 卡片创建/编辑 | 4 |
| | 字典查询 | 2 |
| | 统计图表 | 3 |
| | 导入/导出 | 2 |
| | AI 功能 | 2 |
| | 卡组管理 | 1 |
| **测试阶段** | 功能测试 | 3 |
| | 兼容性测试 | 2 |
| | 性能优化 | 2 |
| **发布阶段** | 小程序审核提交 | 1 |
| | 问题修复与重新提交 | 2 |
| **总计** | | **35 人天** |

### 6.2 时间线规划

```
第 1 周：准备阶段
├─ Day 1-2: 项目初始化、环境搭建
├─ Day 3-4: 后端微信登录接口
└─ Day 5: 域名购买、备案提交

第 2 周：核心功能开发
├─ Day 1-2: 用户认证模块
├─ Day 3-4: 卡片列表/详情
└─ Day 5: 复习功能（基础）

第 3 周：复习功能完善
├─ Day 1-2: 复习功能（手势、动画）
├─ Day 3-4: 卡片创建/编辑
└─ Day 5: 字典查询

第 4 周：扩展功能开发
├─ Day 1-2: 统计图表
├─ Day 3: 导入/导出
├─ Day 4: AI 功能
└─ Day 5: 卡组管理

第 5 周：测试与优化
├─ Day 1-2: 功能测试
├─ Day 3: 兼容性测试
├─ Day 4-5: 性能优化

第 6 周：发布
├─ Day 1-2: 审核提交
├─ Day 3-5: 问题修复（如有）
```

---

## 七、功能裁剪建议

### 7.1 必须保留的功能

| 功能 | 原因 |
|------|------|
| 微信登录 | 小程序核心入口 |
| 卡片浏览 | 核心功能 |
| 复习功能 | 核心功能 |
| 卡片创建 | 核心功能 |
| 字典查询 | 用户体验关键 |
| 基础统计 | 学习动力来源 |

### 7.2 可简化的功能

| 功能 | 简化方案 |
|------|---------|
| SVG 卡片 | 首版使用文字模式，后续迭代添加图片版 |
| 导入/导出 | 首版仅支持导出到微信文件，后续迭代添加云同步 |
| 高级统计 | 首版仅显示基础数据，后续迭代添加图表 |

### 7.3 可延后的功能

| 功能 | 延后原因 |
|------|---------|
| AI 记忆卡 | 非核心功能，可后续版本添加 |
| 批量操作 | 移动端场景较少使用 |
| 卡组分享 | 需要社交功能支持 |

---

## 八、风险评估

### 8.1 技术风险

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| SVG 渲染性能差 | 中 | 高 | 采用图片替代方案 |
| 微信 API 变更 | 低 | 中 | 关注官方更新、预留适配时间 |
| 复杂动画卡顿 | 中 | 中 | 简化动画、使用硬件加速 |
| 内存溢出（大量卡片） | 低 | 高 | 分页加载、虚拟列表 |

### 8.2 合规风险

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| ICP 备案驳回 | 低 | 高 | 提前准备资料、选择可靠服务商 |
| 小程序审核不通过 | 中 | 中 | 阅读审核规范、提前自查 |
| 内容审核问题 | 低 | 中 | 添加敏感词过滤 |

### 8.3 业务风险

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| 用户接受度低 | 中 | 高 | 充分用户调研、灰度发布 |
| 与 Web 版数据不同步 | 中 | 中 | 实现账号绑定、数据同步 |

---

## 九、成本估算

### 9.1 开发成本

| 项目 | 费用 |
|------|------|
| 开发人力（35人天 × ¥800） | ¥28,000 |
| 测试人力 | ¥5,000 |
| 设计费用（UI 调整） | ¥3,000 |
| **小计** | **¥36,000** |

### 9.2 运营成本（年）

| 项目 | 费用 |
|------|------|
| 域名 | ¥100 |
| 云服务器（2核4G） | ¥1,500 |
| SSL 证书 | 免费 |
| CDN 流量（可选） | ¥500 |
| **小计** | **¥2,100/年** |

### 9.3 其他成本

| 项目 | 费用 | 说明 |
|------|------|------|
| 小程序认证 | ¥300/年 | 企业认证 |
| 微信支付（如有） | 0.6% 手续费 | 按交易额 |

---

## 十、结论与建议

### 10.1 最终结论

```
✅ 技术可行性：完全可行
❌ 直接移植性：不可能，需要重写前端
⏱️ 预估工期：4-6 周（1人全职）
💰 预估成本：¥36,000（开发）+ ¥2,100/年（运营）
```

### 10.2 推荐实施路径

```
第一阶段：MVP 版本（4周）
├─ 微信登录
├─ 卡片列表/详情
├─ 复习功能（文字模式）
├─ 卡片创建（基础）
└─ 基础统计

第二阶段：完善版本（2周）
├─ 字典查询自动填充
├─ 图片版卡片
├─ 高级统计图表
└─ 导入/导出

第三阶段：增强版本（2周）
├─ AI 记忆卡
├─ 卡组管理
├─ 数据同步（Web ↔ 小程序）
└─ 成就系统
```

### 10.3 关键决策点

| 决策点 | 建议选择 | 原因 |
|--------|---------|------|
| 开发框架 | **uni-app** | Vue 语法复用、一码多端 |
| 认证方式 | **JWT + 微信登录** | 标准方案、易于维护 |
| SVG 处理 | **后端转图片** | 兼容性最好 |
| 首版功能 | **MVP 精简版** | 快速验证、迭代优化 |

### 10.4 下一步行动

1. **确认决策**：是否启动小程序开发
2. **域名备案**：提前启动（需要 7-20 天）
3. **技术选型**：确认使用 uni-app
4. **开发计划**：制定详细迭代计划
5. **资源准备**：分配开发人员、设计资源

---

## 附录

### A. uni-app 项目初始化命令

```bash
# 安装 HBuilderX 或使用 CLI
npm install -g @vue/cli @dcloudio/uni-cli

# 创建项目
vue create -p dcloudio/uni-preset-vue autor-miniprogram

# 选择 Vue 3 模板
# 选择微信小程序平台

# 运行
npm run dev:mp-weixin
```

### B. 微信小程序配置示例

```json
// project.config.json
{
  "appid": "wx1234567890abcdef",
  "projectname": "autoR",
  "setting": {
    "urlCheck": true,
    "es6": true,
    "enhance": true,
    "postcss": true,
    "minified": true
  }
}
```

```json
// app.json
{
  "pages": [
    "pages/index/index",
    "pages/review/review",
    "pages/cards/cards",
    "pages/cards/create",
    "pages/stats/stats",
    "pages/profile/profile"
  ],
  "tabBar": {
    "list": [
      { "pagePath": "pages/index/index", "text": "首页", "iconPath": "...", "selectedIconPath": "..." },
      { "pagePath": "pages/review/review", "text": "复习", "iconPath": "...", "selectedIconPath": "..." },
      { "pagePath": "pages/cards/cards", "text": "卡片", "iconPath": "...", "selectedIconPath": "..." },
      { "pagePath": "pages/profile/profile", "text": "我的", "iconPath": "...", "selectedIconPath": "..." }
    ]
  }
}
```

### C. 后端新增依赖

```txt
# requirements.txt 新增
djangorestframework-simplejwt==5.3.1
cairosvg==2.7.1  # SVG 转图片
```

---

**文档版本**：v1.0
**最后更新**：2025-01-27
**编写人**：Claude Code AI Assistant
